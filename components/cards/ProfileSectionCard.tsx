
import React, { ReactElement } from 'react'


type ProfileSectionCardProps = {
    header: string;
    text?: string;
    component?: ReactElement
}

const ProfileSectionCard = ({ header, text, component }: ProfileSectionCardProps) => {
  return (
    <div className="">
      
    </div>
  )
}

export default ProfileSectionCard
